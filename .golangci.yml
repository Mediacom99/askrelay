# golangci-lint configuration (schema v2).
# Run via `make lint` (golangci-lint run) and in CI.
version: "2"

run:
  # Build tag for the dev-only instrumentation files (logging, metrics, debug
  # commands) that will live behind `//go:build dev` per pchat-architecture.md §15.
  build-tags:
    - dev

linters:
  enable:
    - govet
    - staticcheck
    - errcheck
    - ineffassign
    - unused
    - revive
  exclusions:
    generated: lax
    presets:
      - comments
      - std-error-handling

formatters:
  enable:
    - gofmt
    - goimports
  settings:
    goimports:
      local-prefixes:
        - github.com/Mediacom99/pchat
