// Package main is the pchat CLI entry point: flag parsing (--room, --ring-size,
// --relay-only, --debug) and wiring together identity, protocol, p2p, and ui.
//
// TODO(pchat): implement CLI entry and wiring per pchat-architecture.md §6 (CLI/Config)
// and §8 (Build & Repo Structure).
package main

func main() {}
