---
title: Reference
description: CLI verbs, the HTTP surface, and the MCP tool surface.
---

The interfaces: the `askrelay` CLI verbs, the relay's HTTP routes (MCP, OAuth, enrollment, health), and the MCP tools an AI client can call.

:::note
This page is being written. The [use cases](/use-cases/) and [overview](/) pages
are complete; the rest of the docs are landing incrementally.
:::
