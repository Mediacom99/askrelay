---
title: Concepts
description: The envelope, the approval gate, identity and devices, threads, and spotlighting.
---

The model behind askrelay: signed envelopes, the two-sided approval gate, per-person device identities and revocation, 1:1 threads, and the inbound-quarantine (spotlighting) that treats every incoming message as untrusted.

:::note
This page is being written. The [use cases](/use-cases/) and [overview](/) pages
are complete; the rest of the docs are landing incrementally.
:::
