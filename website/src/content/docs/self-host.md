---
title: Self-host & operate
description: Deploy behind TLS, configure retention and freshness, back up, and the security must-dos.
---

Running askrelay in production: the single binary and SQLite, a reverse proxy for TLS (Caddy/nginx/Traefik), config flags and environment, retention/freshness knobs, backup of the database **and the signing key**, and the reverse-proxy rate limit that is **required** before enabling browser connectors.

:::note
This page is being written. The [use cases](/use-cases/) and [overview](/) pages
are complete; the rest of the docs are landing incrementally.
:::
