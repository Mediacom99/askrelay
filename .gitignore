# Compiled binaries / build output
/askrelay
/askrelay-dev
/bin/
/dist/
*.exe
*.dll
*.so
*.dylib
*.test
*.out

# Local relay database (created by `serve` with the default -db in the working dir)
/askrelay.db
/askrelay.db-shm
/askrelay.db-wal

# Test & coverage artifacts
coverage.out
coverage.html
*.cover
profile.out
cpu.prof
mem.prof

# Environment / secrets
.env
.env.*
!.env.example

# Editor / OS cruft
.DS_Store
Thumbs.db
*.swp
*.swo
*~
.idea/
.vscode/
*.log

# Session-local material (never tracked)
# /docs/.local/ holds local, uncommitted renders and vendored view assets
/docs/.local/
/prompts/
.claude/memory/
.claude/settings.local.json
/askrelay-checkpoint.html
# /test/ is a throwaway A→B→A message-passing harness (relay db + client configs)
/test/

# Dev-only harness (build tag `dev`, uncommitted)
/devharness
/cmd/devharness/

# Brand-kit archives (assets live unpacked under docs/assets/brand/)
*.zip
