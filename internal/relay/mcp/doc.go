// Package mcp implements the relay's MCP tool surface over stateless
// Streamable HTTP, including per-client profiles and the spotlighting
// renderer (docs/askrelay-architecture.md §5). Implemented by WP-07.
package mcp
